namespace TTNT.Scripts.Player.Roles
{
    public class Detective : BaseRole
    {
        // ADD STUFF THAT OPENS DETECTIVE INFO PAGE WHEN STARTING
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
